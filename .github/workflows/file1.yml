
name: Create Azure Instance

on:
  workflow_dispatch:
    inputs:
      create-instance:
        description: 'Create Azure Instance'
        required: true

jobs:
  create-instance:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
      
      - name: Log in to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      
      - name: Create Azure Instance
        run: |
          az group create --name znode-dev-1-vm_group --location North Central US
          az vm create \
            --resource-group znode-dev-1-vm_group \
            --name myVM \
            --image UbuntuLTS \
            --admin-username azureuser \
            --admin-password MyPassword12345
    
